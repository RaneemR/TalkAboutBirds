<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Users</title>
</head>
<body>
  <h1>Users in <%= users[0].profile %></h1>
  <ul>
    <% users.forEach((user) => { %>
      <li><%= user.username %> - $<%= user.email %></li>
    <% }) %>
  </ul>
  <a href="/">Back to Profile Selection</a>

</body>
</html>